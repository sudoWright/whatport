import WhatPortAppKit

// Public (OSS) stub. All plugin slots stay empty.
// The host app calls this at launch; it does nothing.

@MainActor
public func bootstrapPlugins(registry: PluginRegistry) {
    // No Pro features in the open-source build.
}
